int v [3]'a' 1 2 ;
int a (1);

int mult (int x int y)
{
	return x.y;
}

int main ()
{
	b <- ~2;
	v[1] <- 3;
	t[5] <- h[10];
	b <- x . (8 + 2);
	if (a)
    {
    	b <- 1;
    };
	if (a)
	e <- 2
	else
	g <- 3;

	print v[5];
  	print a;
	print "Digite um numero: \n";
	print "Incrementado algumas vezes a fica " (2+x)a "\n";

	read a;
	read v[b];
	
	c <- mult(3 5);
}